// функции это наши действие в java.script, они иоспользуют разные типы данных и изменяют информацию так как
// мы им укажем
// для начала мы прописуем ключевое слово function, дальше идет имя нашей функции и дальше ставим () и {}

// function showFirstMassage(){
//     console.log('helo world')
// }

// showFirstMassage()  - что бы ее вызвать нужно после всего того обратиться к ней по именни  и поставить()


// function showFirstMassage(){
//     console.log('helo world')
//     let num = 20
// }

// console.log(num)  - если мы создаем переменную внутри функции то больше в коде за пределами функции мы не сможем
// обратиться к этой переменной, и такая переменная называеться локальной потмоу что она существует только
// локально в этой функции, в нутри мы конечно же можем выполнять с переменной какие то действия но 
// можно сделать хитро

// let num = 20

// function showFirstMassage(){   мы можем создать глобальную переменную которую можем использовать везде
//     console.log('helo world')   и просто написал имя переменной со значением в функциию и использовать ее 
//     num = 10;                   в функции так проще и удобно, также можно минять значение переменной
// }                                внутри функции

// но также мы можем сделать так

// let num = 20;

// function showFirstMassage(){   мы можем создать переменную глобальную и создать копию это переменной
//     console.log('helo world')  если быть точнее поместить эту переменную в фунцию и сделть локальной
//     let num = 10;              но при выведение у насс консоль будет обращаться к глобальной переменной
// }                              и не будет видеть локаольную, а локальную мы уже можем изменять внутри функции
//                             функция работает до того как она была вызвана
// console.log(num)

// виды функциий 
// первая обычная 
// FUNCTION DECLARETION преведнная выше

// FUNCTION EXPRESSION существует до своего обявление как переменная var

// СТРЕЛОЧНЫЕ ФУНКЦИИ сокращенный вариант функции 

// const calc = (a, b) => a + b;    пишем переменную с именнем потом присваиваем = значение а потом заместь 

