//по началу создадим какую нибудь переменную которая будет служить нам в качестве дополнения
// ее мы будем использовать например для свзяки с чемто другим
// к примеру создадим переменную и сделамем с ней связку тоесть создадим какойто URL путь что бы
// клиент переходил по нему 
const category = 'toys'; // используем для связки с URL

console.log('htpps://someurl.com/'); // и после последнего слеша нам например нужно перейти в категорию toys
// и что бы это сделать есть старый способ
// для того что бы поставить переменную давно использовали просто + и название переменно
console.log('htpps://someurl.com/' + category);
// но теперь мы можем просто воспользоваться `` такими кавычками и знаком $ c{}
console.log(`htpps://someurl.com/${category}`)

