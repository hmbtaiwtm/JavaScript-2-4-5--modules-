                            // Разница между Массивами и Объектами \\


//Массивы это перечень инофрмации по порядку(0, 1, 2) и порядок имеет очень важное значение по этим номерам
//мы можем обращаться к этим сущнастям(елементов) 
//тоесть ключивая особенность это иммено порядок элементов на него мы всегда орентируемся в массивах
const arr = [1, 2, 3];

//Объекты - структура хранения данных в парном фармате,
//тоесть у нас етсь ключ и у нас есть значение этого ключа
const obj = {a:1, b:2,};

//также к объектам можно добовлять новые что бы это сделать есть 2 способа
const arrObj ={
    a: 'a',
    1: 'b',
    2: 'c'
}
// первый способ добавить новый элемент 
arrObj.b = '1234' //мы обращаемся к объекту и через точку говорим что мы добави новый ключ 'b' и присваиваем
                  // ему значение '1234'
// теперь что вывести нужно прописать 
console.log(arrObj['b']) //кавычки нужны что б сказать Java script  что мы обращаемься к новому елементу а не к переменной
// также можно обратиться через точку
console.log(arrObj.b)

//второй способ создать новы елемент через скобки
arrObj['b'] = 123 //и опять таки нужны кавычки по той же причине что и выше сказаная

//еще в нтури объекта можно создавать еще один объект
const arrObj ={
    a: 'a',
    1: 'b',
    2: 'c',
    df: [{}, {}],
    abc: {     // мы создали объект в нутри объекта и так можно много раз и также можно добовлять Массивы
        
    }
}

